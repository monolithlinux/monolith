cosign.key
cosign.private
/Containerfile
# Secure Boot signing private key (MOK). Lives in the KERNEL_SIGNING_SECRET
# CI secret / locally only; never commit it. Only the .der cert is committed.
MOK.priv
/.iso/
